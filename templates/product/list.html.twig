{% extends 'base.html.twig' %}

{% block title %}Liste des produits{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/home/stage/formation/src/Controller/ProductController.php'|file_link(0) }}">src/Controller/ProductController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/home/stage/formation/templates/product/list.html.twig'|file_link(0) }}">templates/product/list.html.twig</a></code></li>
    </ul>
</div>
<div>
{% for product in products %}
    <div>
    {{ product.name }} vers <a href="{{ path('show', {'id':product.id}) }}">{{ product.name }}</a> edition <a href="{{ path('edit', {'id':product.id}) }}">{{ product.name }}</a> category: <a href="{{ path('editCategory', {'id':product.category.id}) }}">{{ product.category.name }}</a>
    </div>
{% endfor %}

</div>
<div>
<a href="{{ path('create') }}">Création</a>
</div>
<div>
    <button></button>
</div>
{% endblock %}
